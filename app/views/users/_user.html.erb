
		<li>
			<%= gravatar_for user %>
			<%= link_to user.name, user %>
			<% if current_user.admin? %>
			
				<%= button_to "Delete", user_path(user.id) , :remote => true, :onclick => "return confirm('Confirm?');",  :method => :delete %>

         		

				<%= link_to 'delete user', user_path(user.id) ,:method => :delete, :remote => true,  :onclick => "return confirm('Confirm?');", :title => "Delete User" %>
<!-- 				Browser cannot process DELETE request, but Javascript can, then use javascript -->
			<% end  %>
		</li>
	