<!DOCTYPE html>
<html>
	<head>
		<meta http-equiv="Content-type" content="text/html; charset=utf-8">
		<title><%= title_help %></title>
		<%= csrf_meta_tag %>
		<%= render 'layouts/stylesheet' %>
	</head>
	<body id="application.html" onload="">
		<div class="container">
			<%= render 'layouts/header' %>
			<section class="round">
				<%= yield %>
			</section>
			<%= render 'layouts/footer' %>
		</div>
	</body>
</html>